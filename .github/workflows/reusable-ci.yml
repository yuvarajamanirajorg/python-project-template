name: Python Syntax Check with Discord Notification

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  run-syntax-check-template:
    uses: yuvarajamanirajorg/ci-templates/.github/workflows/python-syntax.yml@main
    secrets:
      DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}
